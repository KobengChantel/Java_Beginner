package Book3.chapter5;

// create an abstract method, you specify the modifier abstract and replace the
//method body with a semicolon:
//public abstract int hit(int batSpeed);

//lass that contains at least one abstract method is called an abstract class and must
//be declared with the abstract modifier on the class declaration
public abstract class Ball
{
    public abstract int hit(int batSpeed);
}

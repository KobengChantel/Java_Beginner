package Book3.chapter5;
//ava lets you declare that a method or an entire class is abstract, which means that
//the method has nobody. An abstract method is just a prototype for a method: a
//return type, a name, a list of parameters, and (optionally) a throws clause

public class BaseBall extends Ball
{
    public int hit(int batSpeed) {
// code that implements the hit method goes here
return batSpeed;
    }
    }
